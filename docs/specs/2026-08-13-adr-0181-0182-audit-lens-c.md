# Audit B — Lens C (RE-COUNTING)

| # | Claim (doc + section) | Claimed | Derived | Verdict | How |
|---|---|---|---|---|---|
| C1 | ADR-0181 ctx / spec §2: "There is **one** implementation, not one per dialect" | 1 | 1 (`internal/persistence/store/pruner.go:192`); iface decl `persistence/pruner.go:48` | ✅ CONFIRMED | `grep -rn PruneTimers --include="*.go" .` → only impl is store/pruner.go:192 |
| C2 | ADR-0181/spec §2: predicate is `next_run < cutoff AND trigger_kind IN (Unset, OneTime, Expr)` | 3-kind IN-list | exact: `DELETE FROM wrkflw_timers WHERE next_run < ? AND trigger_kind IN (?,?,?)`, args `nonRecurringTriggerKinds[0..2]` = KindUnset, KindOneTime, KindExpr | ✅ CONFIRMED | pruner.go:198-204, 222-226 |
| C3 | `nonRecurringTriggerKinds` is exactly the complement of `TriggerSpec.Recurring()` | complement | `Recurring()` returns false for `KindUnset, KindOneTime, KindExpr`, true otherwise — identical set | ✅ CONFIRMED | definition/schedule/trigger.go:101-108 |
| C4 | schedule.Kind enumeration (bundle never lists it) | n/a | 10 kinds: Unset=0, OneTime=1, Duration=2, DurationRand=3, Cron=4, Daily=5, Weekly=6, Monthly=7, Expr=8, EveryExpr=9. Recurring(): false for 0,1,8; true for 2,3,4,5,6,7,9 | ℹ️ DERIVED | definition/schedule/trigger.go:13-22 |
| C5 | Plan P3 citation `runtime/timerops.go:86-91` | 86-91 | comment block 86-102; the enumerating sentences are exactly 86-91 | ✅ CONFIRMED | `sed -n '86,102p' runtime/timerops.go` |
| C6 | `neverDueNextRun` doc comment "enumerates THREE guard sites" | 3 | 3 named: `timerJobsFor`, `scheduleStartTimerJob`, `jobStore.Load` ("the two arm sites … The third, jobStore.Load") | ✅ CONFIRMED | timerops.go:88-91 |
| C7 | "There are **four**" guard sites | 4 | 4 exactly: timerops.go:178 (`timerJobsFor`), timerops.go:505 (`scheduleStartTimerJob`), jobstore.go:93 (`jobStore.Load`), processdriver.go:780 (inside `deliverLoop`, post-commit) | ✅ CONFIRMED | `grep -rn neverDueNextRun` (2 hits) + `grep -n "NextRun().IsZero()"` (2 hits) |
| C8 | "two sites call `neverDueNextRun`, two use the equivalent `sj.NextRun().IsZero()`" | 2+2 | 2 + 2 | ✅ CONFIRMED | as C7 |
| C9 | "confirmed by counting the refusal-counter increments (the only exact method)" | 4 | `timerArmsRefused.Add` appears exactly 4× in non-test code: jobstore.go:110, timerops.go:188, timerops.go:506, processdriver.go:794 | ✅ CONFIRMED | `grep -rn "timerArmsRefused" --include="*.go" runtime/` |
| C10 | ADR-0182 / spec §3: `validate.go:656`'s recurring-`DeadlineTimer` check is INSIDE `validateStructure`, not `Build()` | inside | `validateStructure` spans lines **277–757**; the DeadlineTimer loop is 656–661 (`deadline.Recurring()` at 658). Inside. | ✅ CONFIRMED | `grep -n "^func \|^}" definition/model/validate.go` + `sed -n '650,662p'` |
| C11 | spec §3: "the one rule that actually sits in `Validate`" is the root `Version` check | 1 | `Validate` is lines 260–269; body = `d.Version < 1` check + `validateStructure(...)` call + `errors.Join`. Exactly one rule outside the recursion. | ✅ CONFIRMED | `sed -n '260,269p' definition/model/validate.go` |
| C12 | recursion site count into nested sub-processes | (implied 1) | exactly **1**: `validate.go:550` `validateStructure(sub, seen)`, gated `n.Kind() != KindSubProcess`. Event-sub-processes are also `KindSubProcess` (`isEventTriggeredSubprocess`, 767-782), so both nested forms are reached. ⚠ `KindCallActivity`'s referenced definition is **NOT** recursed (only `DefRef != ""` checked, 555-559). | ✅ CONFIRMED (with caveat) | `grep -rn validateStructure definition/` |
| C13 | spec §4.1: ADR-0176's own *Deferred* bullets number four | 4 | exactly 4 bullets under "**Deferred, each for a measured reason:**" (ADR-0176 line 191): model.Validate gate, step-time engine gate, `StepOptions.SchedulingLocation`, moving calendar math to a shared package | ✅ CONFIRMED | `sed -n '191,208p' docs/adr/0176-...md` |
| C14 | spec §4.1: the 4th deferred bullet is "moving the calendar math to a shared package" | yes | yes — 4th bullet verbatim "**Moving the calendar math to a shared package**" | ✅ CONFIRMED | ibid. |
| C15 | spec §4.1: orphan migration lives under *Costs accepted* | yes | yes — ADR-0176 "**Costs accepted:**" (line 173), 4th bullet: "Existing zero-`next_run` rows are not migrated… `PruneTimers` provably cannot." | ✅ CONFIRMED | `sed -n '173,190p'` |
| C16 | spec §4.1: ADR-0176's spec §6.3 lists **six** | 6 | exactly 6 bullets in `docs/specs/2026-08-13-never-due-timer-triggers.md` §6.3 (lines 225-255): Validate gate, step-time gate, SchedulingLocation, calendar-math package, `ErrTriggerNeverDue` sentinel, migrating zero-`next_run` rows | ✅ CONFIRMED | `sed -n '225,256p'` |
| C17 | spec §4.1 recap: "The count **four** appears in **no single source document**." | — | **FALSE.** ADR-0176's Deferred list is exactly four bullets — the very list the same sentence enumerates two lines earlier. What is unique to `HANDOVER.md` is the *membership* (it swaps calendar-math for orphan migration), not the count. | ❌ WRONG (Minor) | C13 + `sed -n '236,244p' docs/plans/HANDOVER.md` |
| C18 | HANDOVER's inherited "four gates" membership | — | HANDOVER.md:97-99 and :238-243 both list: model.Validate gate, step-time gate, `StepOptions.SchedulingLocation`, **migrating orphan rows** — i.e. calendar-math replaced by orphan migration. The bundle's diagnosis of the merge is correct. | ✅ CONFIRMED | ibid. |
| C19 | spec §5: "all **868** construction sites are keyed; exactly **one** is non-test" | 868 / 1 | `grep -rn "StepOptions{" --include="*.go" .` → **868**; non-test → exactly **1** (`runtime/processdriver.go:615`); 821 are `StepOptions{}`, the other 47 all keyed, zero positional literals | ✅ CONFIRMED | greps above |
| C20 | `StepOptions` field count (asked, not claimed in bundle) | — | **6**: `Mode`, `DefaultRetryPolicy`, `OverrideRetryPolicy`, `Evaluator`, `IDGenerator`, `CompensationStallAfter` | ℹ️ DERIVED | `engine/step.go:24-68` |
| C21 | `StepOptions` sites in `examples/` | (implied 0) | **0** — `grep -rn StepOptions examples/` returns nothing | ✅ CONFIRMED | grep |
| C22 | ADR-0181/spec §2: "**every never-due kind is recurring**" / sets disjoint **by construction** | universal | HOLDS for every armable kind. `convertTrigger` (runtime/timerops.go:44) is total over the 10 kinds; Unset/Expr/EveryExpr error out and never arm. Of the 7 armable, only `KindOneTime` is non-recurring, and it maps to `At(at)` (at ≠ zero by the `AbsTime()` guard → `(at,true)`) or `After(d)` (`after.Add(d)`, always ok, non-zero). So no non-recurring kind can yield `!ok` or a zero instant. | ✅ CONFIRMED | scheduler/trigger.go:245-290, runtime/timerops.go:44-73, definition/schedule/trigger.go:101-108 |
| C23 | `schedule` constructor count | — | **10**: AfterDuration, At, AfterExpr, Every, EveryExpr, EveryRandom, Cron, Daily, Weekly, Monthly | ℹ️ DERIVED | `grep -n "^func [A-Z]" definition/schedule/trigger.go` |
| C24 | ADR-0182 Decision: never-due class list is complete for the anchor-independent class | 6 items | **INCOMPLETE — 4 omissions + 1 over-reach.** See rows C25–C29. | ❌ WRONG (Critical) | scheduler/trigger.go:245-290, 383-392, 485-501, 570-591 |
| C25 | ADR-0182 lists `Every(0)` | — | actual predicate is `t.dur <= 0` — **`Every(negative)` is equally never-due** and is not named | ❌ NARROW (Major) | scheduler/trigger.go:255-258 |
| C26 | ADR-0182 lists `EveryRandom(min >= max)` (spec §4.4 writes `min>max` — a third variant) | — | actual predicate is `t.min <= 0 \|\| t.min >= t.max` — **`EveryRandom(0, 5s)` is never-due** (min<max) and is not named | ❌ OMISSION (Major) | scheduler/trigger.go:259-268 |
| C27 | ADR-0182 lists zero-interval for `Every`, `Daily`, `Weekly` | 3 | `calendarNext` returns `!ok` for `interval == 0` on **Daily, Weekly AND Monthly**. **`Monthly(0, …)` is never-due and is absent from the list.** | ❌ OMISSION (Major) | scheduler/trigger.go:384-386 |
| C28 | ADR-0182 lists "an **unparseable** cron" | — | Two distinct cron never-due causes: (a) parse error, (b) **parseable but matching no instant** — robfig searches 5 years and returns the ZERO time with no error. `Cron("0 0 30 2 *")` is case (b), is the bundle's own §3 motivating example, and is anchor-independent — yet "unparseable cron" does not cover it and "out-of-range day-of-month" does not either (30 is a legal day-of-month; it is the month×day *combination* that is empty). | ❌ OMISSION (Critical) | scheduler/trigger.go:269-286; spec §3 line 98 |
| C29 | ADR-0182 lists "out-of-range … **weekday**" | reject | **Over-reaches into unsoundness.** Only a NEGATIVE weekday is never-due. A weekday **above Saturday** "stays a raw day offset, so it always matches on the first pass" — it DOES fire. Rejecting "out-of-range weekday" as written refuses a spec that fires. | ❌ UNSOUND (Critical) | scheduler/trigger.go:474-484, 485-501 |
| C30 | Plan P1 step 1 fixture: "four never-due recurring rows (`Every(0)`, `Cron` bogus, `Monthly(12,[31])`, `Weekly(1,nil)`)" | 4 never-due | **`Weekly(1, nil)` is NOT never-due.** `weeklyNext` substitutes an empty weekday set with **Sunday**, mirroring the gocron adapter — it always finds a next Sunday. Only 3 of the 4 named specs are never-due. | ❌ WRONG (Major) | scheduler/trigger.go:481-488 |
| C31 | ADR-0182 lists "out-of-range … day-of-month" and "clock fields" | reject | ✅ sound: `monthDaysSchedulable` rejects `d == 0 \|\| d > 31 \|\| d < -31`; `clockTimesSchedulable` rejects `Hour>23 \|\| Minute>59 \|\| Second>59`. Both are genuine never-due, both directions. | ✅ CONFIRMED | scheduler/trigger.go:570-591 |
| C32 | ADR-0182 / spec §3: `scheduler/selfcontainment_guard_test.go` exists and protects the spin-out | exists | exists, 131 lines, 2 tests. `TestSchedulerTreeIsSelfContained` bans any **non-test** file under `scheduler/...` from importing a `github.com/kartaladev/wrkflw/*` package outside `scheduler/...`. `TestSchedulerBackendsHaveNoDirectDBDeps` confines `database/sql`/pgx to 4 named elector packages. | ✅ CONFIRMED (⚠ direction) | file read in full |
| C33 | The guard's DIRECTION vs how the bundle leans on it | — | ⚠ The guard is **outbound-only**: it constrains `scheduler → wrkflw/*`. It says **nothing** about `definition/model → scheduler`. It genuinely forbids moving calendar math into a shared `wrkflw/*` package (§4.4 correct), and it is evidence the spin-out is a live plan — but it does **not** "protect" against the rejected alternative. Wording invites the mis-read. | ⚠️ IMPRECISE (Minor) | ibid. |
| C34 | ADR-0182: making `definition/model` depend on `scheduler` would be a NEW coupling | new | ✅ `grep -rl kartaladev/wrkflw/scheduler definition/` → **no matches** (EXIT=1). Today's non-scheduler importers: `persistence`, `processtest`, `runtime`, `runtime/internal/runtimetest`, + 11 `examples/*`. `definition/*` is genuinely not among them. | ✅ CONFIRMED | grep |
| C35 | spec §3 / ADR-0182: "`model → event` is a real import cycle" | cycle | ✅ `definition/event/event.go:10` imports `definition/model` (production). So `model → event` would close a cycle. (`definition/model`'s `event` imports are all in `_test.go` external test packages — no production import.) | ✅ CONFIRMED | grep |
| C36 | spec §3 line 142: "`PutDefinition`/`GetDefinition` **never** call `Validate`" | never | ✅ `grep -n Validate internal/persistence/store/definitions.go` → EXIT=1, zero hits. Repo-wide, the **only** non-test caller of `model.Validate` is `definition/model/builder.go:133`. | ✅ CONFIRMED | grep |
| C37 | spec §3 line 143: "`Build()` reaches `validateStructure`" | yes | ✅ `definitionBuilder.Build()` (builder.go:198) → `build()` → `Validate(&def)` (builder.go:133) → `validateStructure`. `definition/build.Builder.Build()` (build.go:163) delegates to it. | ✅ CONFIRMED | grep |
| C38 | spec §3 line 106: "A rule outside `validateStructure` exempts **every** nested sub-process" | every | ✅ `Validate` is called only on the root (single call site, builder.go:133); nested definitions are reached only through `validateStructure`'s own recursion (validate.go:550). | ✅ CONFIRMED | greps C36/C12 |
| C39 | ADR-0182 Decision: "a standalone predicate over `schedule.TriggerSpec`, evaluated … **via `toWire(n).TimerTrigger`**" | type | ⚠ **Type mismatch as written.** `toWire(n).TimerTrigger` is `*model.TriggerWire` (`node_wire.go:42`), not `schedule.TriggerSpec`. The conversion exists — `model.ReadTrigger(w *TriggerWire, flatExpr string, recurringFlat bool) schedule.TriggerSpec` (`trigger_wire.go:60`) — but the ADR and plan P2 step 2 both omit it. An implementer following the text literally gets a compile error. | ⚠️ IMPRECISE (Minor) | node_wire.go:42, trigger_wire.go:25-60 |
| C40 | (not claimed) second timer representation on a node | — | ⚠ `validate.go:301` reads `w.TimerTrigger != nil \|\| w.TimerDuration != ""` — there is a **legacy flat `TimerDuration` string** form the gate would not see if it reads only `TimerTrigger`. Harmless for never-due (a duration string decodes to `AfterDuration`, non-recurring, always due) but the bundle never mentions the second form. | ℹ️ GAP (Minor) | validate.go:301, 529-531 |
| C41 | (not claimed) how many trigger-carrying fields a node has | bundle names **1** | **3**: `TimerTrigger`/`TimerDuration`, `DeadlineTrigger`/`DeadlineDuration` (→ `DeadlineTimer`), `WaitTrigger`/`WaitEvery` (→ `WaitEvery`) — `node_wire.go:38-44`, decoded at `node_wire.go:119-122`. The gate as specified reads only `TimerTrigger`. `DeadlineTimer` is genuinely covered (recurring already rejected at validate.go:656, and non-recurring can never be never-due — C22). **`WaitEvery` is NOT covered and IS exposed**: `armWaitReminder` (`engine/step_nodes.go:689-712`) emits a `ScheduleTimer{Kind: TimerInWait}` from it, so `WaitEvery = Every(0)` / `Monthly(0,…)` reaches the identical durable-arm path the gate exists to guard. | ❌ OMISSION (Major) | node_wire.go, engine/step_nodes.go:689-712 |
| C42 | ADR-0181: `ASSUMPTION (unverified — needs Docker): the same 0-of-4 result holds on Postgres and MySQL` | unverified | **Not merely unverified — the MySQL half is REFUTED by a measurement in a document this bundle cites.** ADR-0176's spec §2 measured MySQL **REJECTING** a zero `next_run` at `UpsertJob` (`Error 1292 (22007): Incorrect datetime value: '0000-00-00'`). So on MySQL orphan rows **cannot exist**, the P1 fixture **cannot be seeded**, and the new `next_run = <zero>` sweep is a structural no-op. The evidence file's "`PruneTimers` cannot delete a single orphan, at any cutoff, **on any dialect**" is vacuously true on MySQL for a reason it never states. | ❌ WRONG (Critical) | `docs/specs/2026-08-13-never-due-timer-triggers.md:44-58` |
| C43 | spec §4.4: "the `ErrTriggerNeverDue` sentinel **exists**" | exists | **No such symbol exists.** `grep -rn ErrTriggerNeverDue --include="*.go" .` → EXIT=1, zero hits. ADR-0176's spec §6.3 said it "already exists **in substance**" — the refusal at `scheduler/scheduler.go:580` wraps `ErrUnsupportedTrigger` with the text *"trigger can never fire"*. **The hedge was stripped on restatement.** | ❌ WRONG (Major) | grep; ADR-0176 spec §6.3 |
| C44 | inherited citation `scheduler/scheduler.go:575-578` (from ADR-0176 spec §6.3) | 575-578 | **rotted** — the refusal is now at **578-581** (`Next` call 578, `if !ok` 579, return **580**). | ❌ STALE (Minor) | `grep -n "trigger can never fire" scheduler/scheduler.go` |
| C45 | `EveryRandom` never-due predicate — three different spellings across the bundle | — | spec §4.4 `min>max` · ADR-0182 + spec §3 `min >= max` · plan P2 `min>=max` · **actual: `t.min <= 0 \|\| t.min >= t.max`**. No two agree with the source. | ❌ INCONSISTENT (Major) | scheduler/trigger.go:259-268 |
| C46 | plan ▶ Progress: "B is `definition/model` and `internal/persistence/store`" (packages touched) | 2 | **3** — the plan's own execution table (line 28) assigns **P3 to `runtime`**. The two-package statement contradicts the table four lines below it, and it is the sentence the "bundles touch disjoint packages" independence argument rests on. | ❌ WRONG (Major) | plan lines 12-14 vs 24-28 |
| C47 | plan line 20-21: "**the two phases** are in different Go packages" | 2 | **3 phases** (P1, P2, P3). Recap sentence undercounts. | ❌ WRONG (Minor) | plan lines 20-28 |
| C48 | plan P3 doc-only scope: **one** comment to correct | 1 | **at least 2 shipped comments go stale.** `runtime/jobstore.go:48-49` currently reads "Nothing reclaims a skipped row: it stays in the timer store until an operator removes it, and **`Pruner.PruneTimers` does not delete it**" — ADR-0181 makes the first half false. P3 names only `neverDueNextRun`. (`runtime/timerops.go:237` "`Pruner.PruneTimers` reclaims exactly these rows" stays true — one-shots are in the IN-list — but "exactly" is worth re-reading once a second sweep exists.) | ❌ OMISSION (Major) | `sed -n '45,50p' runtime/jobstore.go`, `sed -n '235,240p' runtime/timerops.go` |
| C49 | evidence file §4's `schedule.Kind` enumeration + "every never-due kind is recurring → 2,3,4,5,6,7,9" | 10 kinds | reproduced **exactly**, independently | ✅ CONFIRMED | definition/schedule/trigger.go:13-22 |
| C50 | evidence file §5: 4 guard sites, 4 refusal increments, **3** scheduler-entry sites in `runtime`, "no unguarded arm path" | 4/4/3 | reproduced **exactly**: `grep "\.Schedule(\|\.Activate("` non-test, excluding `/scheduler/` and runtimetest → timerops.go:385, timerops.go:517, processdriver.go:801 (+ scheduler.go:597 inside the façade). | ✅ CONFIRMED | grep |
| C51 | spec §4.4 / ADR-0176: shared calendar package "forbidden by the self-containment guard" | forbidden | ✅ correct — moving calendar math to a `wrkflw/*` package outside `scheduler/...` would force `scheduler` production code to import it, which `TestSchedulerTreeIsSelfContained` fails on. | ✅ CONFIRMED | selfcontainment_guard_test.go:42-63 |
| C52 | plan P1 **Verify** command `go test -count=1 -run '^TestPrune' -v ./internal/persistence/store/` vs plan line 30 "P1 needs Docker **only** for its cross-dialect claim" | SQLite-only | **Contradiction.** `^TestPrune` matches `TestPruner` (`pruner_conformance_test.go:30`), whose every subtest runs under `forEachDialect` → Postgres + MySQL + SQLite. The prescribed per-phase loop **requires Docker**. Compounds C42: an orphan-sweep subtest added under `TestPruner` will be run on MySQL, where the zero-`next_run` fixture cannot be inserted. | ❌ WRONG (Major) | pruner_conformance_test.go:30-39 |
| C53 | `runtime/timerjob.go:71` shipped comment "**both callers** have already refused … timerJobsFor … jobStore.Load" | 2 | 2 non-test callers of `newScheduledTimerJob`: `timerops.go:282` (in `buildTimerJob`, reached from `jobStore.Load` at jobstore.go:82) and `processdriver.go:744` (commit closure, jobs from `timerJobsFor`). Provenance labelling is accurate. | ✅ CONFIRMED | grep |
| C54 | spec §7: `RunTestSQLite` is pure-Go, starts no container | yes | ✅ `internal/dbtest/sqlite.go:22 func RunTestSQLite(t *testing.T) *sql.DB` | ✅ CONFIRMED | grep |
