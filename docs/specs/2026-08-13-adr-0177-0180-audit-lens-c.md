# Audit lens C — RE-COUNTING (ADR-0177..0180 bundle)

| # | Claim (doc + loc) | Claimed | Derived | Verdict | How |
|---|---|---|---|---|---|
| C1 | spec §3 "wired at all FOUR dispatch sites: step_compensation.go:412, :574, :1301, step_nodes.go:1134" (also plan P2.9) | 4 | **4** | ✅ CONFIRMED | `grep -rn compensationInvoke --include='*.go' .` → non-test call sites exactly step_compensation.go:412,574,1301 + step_nodes.go:1134 (500/503 are doc+def). Cross-check `ActiveCmdID =` → 405, 567, 1296, step_nodes.go:1126 — same four walks. |
| C2 | spec §6 row 5 "`armCompensationStallTimer` has **four** call sites" | 4 | **4** | ✅ CONFIRMED | `grep -rn armCompensationStallTimer` → 415, 577, 1302 (step_compensation.go) + step_nodes.go:1139; 460=doc, 471=def. Zero test call sites. |
| C3 | spec §3 "a new incident kind must be retired at all FOUR retirement sites (`:524`, `:1287`, `:1294`, `:1345`)" | 4 | **5** | ❌ **WRONG — undercount** | `grep -rn retireCompensationStallIncidents` → step_compensation.go:524,1287,1294,1345 **+ engine/state.go:475** (the `endInstance` REMAINDER sweep, documented at state.go:415-419 as "plus a final sweep in endInstance"). |
| C4 | spec §9.3(d)/plan P2.4 "exclude the new kind from **the** cause-of-death publication (`:1338-1344`)" — implies ONE site, in `engine` | 1 site, pkg `engine` | **2 sites, pkg `runtime`** | ❌ **WRONG** | `grep -rn "terminalEventErr\|func terminalErr"` → `runtime/outbox.go:81` and `runtime/processdriver_action.go:31`, both `if len(st.Incidents) > 0 { return st.Incidents[0].Error }`. `engine/step_compensation.go:1338-1344` is only a COMMENT describing them. Plan phase table lists P2 package as `engine` only and P2 Verify is `go test ./engine/...` — neither reaches the edit. |
| C5 | ADR-0178:16 "`spawnsNewWork()` has **six** production call sites" | 6 | **6** | ✅ CONFIRMED | `grep -rn spawnsNewWork` non-test: step_arm_dispatch.go:54, step_triggers.go:564/939/957/1139, step_eventsubprocess.go:172. (state.go:534 def; export_test.go:141 is a test helper.) |
| C6 | spec §2 / ADR-0178 "`handleTimerFired` has **five** numbered paths" | 5 | **5** | ✅ CONFIRMED | step_triggers.go:498-503 comment enumerates 1)–5); code has cascade (1-3), `rec != nil` switch (4), `tokenAwaiting` fall-through (5). |
| C7 | spec §2 "paths 1,2,3,5 guarded by `spawnsNewWork()`; **path 4 is the only unguarded one**" | 4 of 5 guarded | **4 of 5** | ✅ CONFIRMED | 1-3 guarded inside `dispatchArmCascade` (step_arm_dispatch.go:54, ahead of every lookup); 5 guarded at step_triggers.go:564; the `rec != nil` switch at :529-540 has no guard. ⚠ wording nit: the guard for 1-3 is in `dispatchArmCascade`, not in `handleTimerFired` — ADR-0178:16 says "handleTimerFired guards four of its five". |
| C8 | ADR-0178:22 "all **four** `return`s in the switch jump over it" | 4 | **4** | ✅ CONFIRMED | step_triggers.go:530-539: cases TimerDeadline/TimerInWait/TimerRetry/TimerCompensationStall, one `return` each. |
| C9 | spec §2 "`slog.WarnContext` (already the house idiom — **six** `engine` files use it)" / ADR-0178:58 same | 6 | **4** | ❌ WRONG (minor) | `grep -rln slog.WarnContext engine/ \| grep -v _test` → 4 files (step_triggers, step_compensation, step, step_stale_commands). SIX is the count of files using **any** `slog.` (adds trigger_validate.go, step_errors.go). |
| C10 | spec §1 / ADR-0177:20 "**eight** production arm sites" | 8 | **8** | ✅ CONFIRMED | `grep -rn "ScheduleTimer{" --include='*.go' . \| grep -v _test` → step_boundaries.go:54, step_compensation.go:493, step_triggers.go:328, step_eventsubprocess.go:97, step_nodes.go:699/772/831/988 = 8. |
| C11 | spec §1 / ADR-0177 "**five** `TimerKind` constants" | 5 | **5** | ✅ CONFIRMED | engine/command.go const block: TimerIntermediate, TimerDeadline, TimerInWait, TimerRetry, TimerCompensationStall. (⚠ ADR-0177 cites `command.go:17-41`, evidence cites `:20-41`; actual const block is 19-40 — cosmetic line rot.) |
| C12 | 0177 evidence "**Writers of `s.Timers`: FOUR**" | 4 | **4** | ✅ CONFIRMED | Only appends of new records: step_nodes.go:705, :778, step_triggers.go:334, step_compensation.go:478. Rebuild/clear sites (state_timers.go:61/81/128, step_compensation.go:453/456) and the snapshot copy (step_state.go:395) are correctly excluded and the exclusion is stated. |
| C13 | 0177 evidence "**Readers of `s.Timers` — SEVEN** production sites, all in `engine`" | 7 | **7** | ✅ CONFIRMED | timerByID, removeTimer, cancelTimersWhere, cancelAllTimers, HasArmedTimers, cancelCompensationStallTimers, cloneState. `grep -rn '\.Timers' engine/ \| grep -v _test` shows no eighth reader. |
| C14 | 0177 evidence:113 "`grep -rn '\.Timers' runtime/ service/ transport/ internal/ processtest/` **→ none in production code**" | none | **3 hits** | ⚠ CLAIM-AS-LABELLED FALSE, conclusion true | Real grep returns transport/http/{fiber:423-424, stdlib:417-418, gin:432-433}/groups.go. Those are the timer-**admin dependency** field (`g.Timers`→`ta`), not `InstanceState.Timers`, so "no consumer outside `engine` reads `InstanceState.Timers`" (spec §1 Blast radius) is **TRUE**; only the printed grep result is wrong. |
| C15 | 0178-0180 evidence:140 "6 terminal outcomes … plus the `tok == nil` no-op at `:548` and **three** `return StepResult{}, err` error exits" | 6+1+3 = 10 returns | **11 returns** | ❌ WRONG (minor, evidence only) | `handleTimerFired` returns at 518, 520, 532, 534, 536, 538, 548, **565**, 576, 580, 582 = 11. The accounting omits `:565`, the `!spawnsNewWork()` path-5 no-op — ironic, since that guard is ADR-0178's whole subject. The 3 error exits (518/576/580) and the 6-outcome definition are correct. |
| C16 | spec §1 "`HasArmedTimers()` has **two** call sites repo-wide: `processtest/park.go:141` (prod) + `engine/step_compensation_stall_test.go:378` (test); **zero** in runtime/service/transport/internal/examples" | 2 | **2** | ✅ CONFIRMED | `grep -rn HasArmedTimers` — only those two *invoke the method*; all other hits are the distinct `processtest.Park.HasArmedTimers` **field** (park.go:100/141/175, harness.go:312, tests). |
| C17 | spec §1 "`processtest.Classify`'s reason priority … `terminal > openTasks > incidents > signals > messages > HasArmedTimers > commandWait > unknown`, source-verified at `processtest/park.go:159-186`" | that order | **identical order** | ✅ CONFIRMED | park.go switch at 160-185: IsTerminal(161) → OpenTasks(163) → Incidents/hasIncidentToken(166) → AwaitingSignals(169) → AwaitingMessages(172) → HasArmedTimers(175) → hasCommandWait(178) → default Unknown(183). Line range `159-186` is right (159 = the "in priority order" comment). |
| C18 | spec §6 "of **25** `§` citations in Go code" | 25 | **25** | ✅ CONFIRMED | `grep -rn "§" --include='*.go' . \| wc -l` → 25. |
| C19 | spec §6 row 6 "ADR-0034 has **zero** `§`" and "exactly **four** Decision items" | 0 § / 4 items | **0 / 4** | ✅ CONFIRMED | `grep -c '§' docs/adr/0034-*.md` → 0. Decision section numbers items 1–4 (lines 21, 26, 30, 32). |
| C20 | spec §6 row 6 "the form already used at **four** other Go sites (`ADR-0034 Decision 4`)" | 4 | **4** | ✅ CONFIRMED | `grep -rn "Decision 4" --include='*.go' .` → ADR-0034-scoped hits: step_compensation_stall_verbs_test.go:60, step_compensation_stall_incident_test.go:140, step_compensation.go:521, trigger.go:920. (Other "Decision 4" hits cite ADR-0169/0172, not 0034.) |
| C21 | spec §6 "⚠ #6 is a lone slip … **the only one attaching a `§` to an ADR that has none**" | only one | **true literally; misleading** | ⚠ TRUE-AS-WORDED, weak | ADR-0034 is the only cited ADR with zero `§` (0176→8, 0174→1, 0165→1). BUT the § in those ADRs are citations *to other docs*, not self-sections: `scheduler/trigger.go:382` + `trigger_test.go:554` say "(ADR-0176 §4)" while ADR-0176's headings are only Context/Decision/Consequences; `state_compensation.go:391` says "ADR-0174 §5.3" while ADR-0174's only § is a quote of "the spec's §5.3.1". So **three further dangling ADR-§ references exist** of the same class; the "lone slip" framing is the over-generalising recap Premise Discipline warns about. Not a blocker for this bundle (out of scope), but the §6 sentence should be narrowed. |
| C22 | spec §6 row 7 / ADR-0034 "`docs/adr/0034-…:51` — 'logged/skipped'" | line 51 | **line 51** | ✅ CONFIRMED | `grep -n "logged/skipped" docs/adr/0034-*.md` → 51. |
| C23 | spec §4b / ADR-0180:59 "`handleCancelRequested` has **five** return sites: 1 error, 2 doing real work, **2 returning nil without terminating** (`:196` deferred, `:210` dropped)" | 5 (2+2+1) | **5 (2+2+1)** | ✅ CONFIRMED | returns at 196, 210, 234 (`StepResult{}, err`), 250 (`res, nil` compensation branch), 272 (immediate terminate). Line numbers `:196` and `:210` are exact. |
| C24 | ADR-0180:41 "Of **seven** entry points that can start an instance, **two are unguarded** … three guarded by id-uniqueness, two by fresh-id minting" | 7 / 2 / 3 / 2 | **7 / 2 / 3 / 2** | ✅ CONFIRMED | `grep -rn NewStartInstance --include='*.go' . \| grep -v _test` → processdriver_child.go:60, processdriver.go:448, :484 (+ trigger_codec.go:158, correctly excluded as journal replay). Plus caller-built triggers via `engine.Step` (#1) and `ProcessDriver.ApplyTrigger` (#2). Spot-checked the two `service` ApplyTrigger call sites: `service.go:377` builds `SignalReceived`, `:589` is `deliverTaskTrigger` — neither can carry a `StartInstance`, so no eighth entry point. |
| C25 | ADR-0180:41 "**No HTTP route accepts an arbitrary trigger**" | none | **none** | ✅ CONFIRMED | Only `service.deliverTaskTrigger` takes `engine.Trigger` and it is unexported + human-task-only; transport delegates to typed service methods. |
| C26 | spec §6 row 1 site `engine/state.go:225` | :225 | **:226** | ⚠ line rot | `grep -n "auxiliary bookkeeping table" engine/state.go` → 226. Claim itself ("**all** scheduled timers" is false) is CORRECT. |
| C27 | spec §6 row 2 site `engine/state_timers.go:138`; "`TimerKind` and the field are exported; only the struct type is [unexported]" | :138 | **:138, and correct** | ✅ CONFIRMED | state_timers.go:11 `type timerRecord struct` (unexported) with **exported** field `Kind TimerKind` (:15); `InstanceState.Timers` is exported. `st.Timers[i].Kind` is legal from an external package. |
| C28 | spec §6 row 3 site `engine/state_timers.go:141-145`: names **three** invisible sources, says "all four" | 3 named / "all four" | **3 named / "all four sources"** | ✅ CONFIRMED | :142-145 "a timer armed as a BOUNDARY, event-gateway or event sub-process arm is invisible … TimerWaiters() enumerating **all four sources**". |
| C29 | spec §6 row 4 site `engine/step_triggers.go:524-526`, stall dispatched "**twelve** lines below at `:537`" | 12 | **13** | ⚠ off-by-one (cosmetic) | comment at 524-527; `case TimerCompensationStall:` is at 537 → 537−524 = 13. Substance (the comment omits `TimerCompensationStall`) is CORRECT. |
| C30 | spec §6 row 5 site `engine/step_nodes.go:1135-1136` "the third of the three compensation dispatch sites (ADR-0175)" | :1135-1136 | **:1135-1136** | ✅ CONFIRMED | verbatim; and false — 4 dispatch sites (C1) and 4 arm sites (C2). The comment's next clause "Arming only the other two" is wrong for the same reason. |
| C31 | spec §6 row 6 site `engine/step_triggers.go:291` "(ADR-0034 §2.5)" | :291 | **:291** | ✅ CONFIRMED | verbatim. Also confirmed `docs/specs/2026-06-23-compensation-on-error-cancel-design.md` really is the doc with a `### 2.5` (bundle's "not invented" note). |
| C32 | spec §6 "**six** false claims in shipped code" — is there a **SEVENTH**? | 6 | **at least 8** | ❌ **UNDERCOUNT — two more stale kind enumerations, both in files this bundle edits** | (a) `engine/state_timers.go:14` — *"Kind discriminates intermediate, deadline, and in-wait timers."* names 3 of 5 kinds, omits `TimerRetry` + `TimerCompensationStall`, **and** names `TimerIntermediate` which by this bundle's own finding never lands in a record. (b) `engine/command.go:66` — *"Kind distinguishes intermediate, deadline, in-wait, and retry timers"* names 4 of 5, omits `TimerCompensationStall`. Both are the exact class of §6 row 4, and **ADR-0179's new `TimerCompensationRetry` makes both strictly more wrong**. |
| C33 | spec §7 six HANDOVER quotes resolve to `docs/plans/HANDOVER.md` | 6/6 | **6/6** | ✅ CONFIRMED | :139 (#1), :137-138 (#2, paraphrased not verbatim), :168-169 (#3), :217-218 (#4), :219-220 (#5), :154-157 (#6). |
| C34 | spec §7.2 correction "`harnessEnv.classify` overrides the field to `true` when a parked token's `AwaitCommand` matches a pending scheduler timer" | true | **true** | ✅ CONFIRMED | `processtest/harness.go:300-318`: `if _, ok := e.h.sched.Pending(tok.AwaitCommand); ok { p.HasArmedTimers = true; … }`. Receiver is a **value** `harnessEnv`, not `*harnessEnv`. |
| C35 | all named symbols exist / `walkScoped` does NOT | — | **as claimed** | ✅ CONFIRMED | `consumeDispatchedRecord` state_compensation.go:573 · `retryStalledCompensation` step_compensation.go:1273 · `effectiveRetryPolicy` step_state.go:292 · `abandonCompensationWalk` step_compensation.go:1308 · `ErrCompensationWalkResumes` errors.go:109 · `harnessEnv.classify` harness.go:300 · `firstNodeWhere` park.go:304 · `hasCommandWait` park.go:295 · `TestStallIncidentIsRaisedOnADyingWalk` step_compensation_stall_incident_test.go:89. **`walkScoped` → ZERO hits repo-wide** (bundle proposes it). |
| C36 | spec §5.2 "The `walkScoped()` predicate that `HasArmedTimers()` **already applies** to `TimerCompensationStall`" | already exists | **does not exist** | ❌ WRONG (premise) | `HasArmedTimers()` today is an inline `if tr.Kind != TimerCompensationStall` (state_timers.go:146-152); there is no `walkScoped()` anywhere. Plan P1.6 correctly says "**Introduce** `TimerKind.walkScoped()`" — the spec sentence contradicts it. |
| C37 | spec §1 "**Four** sites arm as `TimerIntermediate` and are **never** written into `s.Timers`; **four** are … the guard/no-guard split is exactly the record/no-record split" | 4 / 4 | **4 / 4** | ✅ CONFIRMED | `grep -n "Kind:" … \| grep -i timer`: TimerIntermediate at step_eventsubprocess.go:101, step_boundaries.go:58, step_nodes.go:835, step_nodes.go:992 (= the 4 unrecorded arm sites); TimerInWait/TimerDeadline/TimerRetry/TimerCompensationStall each appear as a record-append + ScheduleTimer pair (= the 4 recorded sites). |
| C38 | spec §3 "`grep -c slog` over `stepCompensationAdvance` and `handleActionFailed` both return **0**" | 0 / 0 | **0 / 0** | ✅ CONFIRMED | `awk '/^func stepCompensationAdvance/,/^}/' … \| grep -c slog` → 0; same for `handleActionFailed`. |
| C39 | spec §1 "widening does not reclassify **the measured boundary/gateway/ESP fixtures — all three** park with an awaited message and stay `ReasonMessage`" | 3 measured | **3 at harness level (probe 2 B/C/D); but 4 boundary-family fixtures were measured overall** | ⚠ IMPRECISE RECAP | Probe 2 measured exactly B (boundary/receive), C (event-gateway), D (ESP) → all `Reason = message`, so "all three" is right *for probe 2*. But §1's own fixture table also lists "**boundary timer on a user task**" (probe 1 case A), whose non-reclassification comes from the **openTasks** rung, not messages. §9.1(b)'s regression test only pins the message rung, so a ladder reorder that moves timers above openTasks would go undetected. |
| C40 | ADR-0177:39 "The plain intermediate catch event appears in **no enumeration anywhere**, including `HANDOVER.md`" | none | **2 counter-examples** | ❌ WRONG (over-reaching recap) | `engine/command.go:66` — "Kind distinguishes **intermediate**, deadline, in-wait, and retry timers"; `engine/state_timers.go:14` — "Kind discriminates **intermediate**, deadline, and in-wait timers". Both enumerate it. True only under the narrow reading "no enumeration *of invisible timer-arm sources*" — which is what the sentence should say. |
| C41 | ADR-0179:84 "the silent-failure hole closes in **all three channels** at once" (spec intro: "in every channel") | 3 | **unenumerated** | ⚠ soft quantifier | Neither the ADR nor the spec names the three. Inferable as log / incident / retry, but per Premise Discipline "prefer naming a closed set over counting it". |
| C42 | spec §4b cites `runtime/processdriver_cancel.go:80-88` for "logs-and-continues" | :80-88 | **:83-90** | ⚠ line rot (cosmetic) | the `applyTrigger(NewCancelRequested)` + Warn + `continue` block is 83-90. Substance correct. |
| C43 | spec §4b / ADR-0180 "`CancelRequested.terminalPolicy()` is `rejectSilently`" | rejectSilently | **rejectSilently** | ✅ CONFIRMED | `engine/trigger.go:833`. |
| C44 | spec §2 cites `engine/step_compensation_stall_incident_test.go:88-102` for `TestStallIncidentIsRaisedOnADyingWalk` and its fixture | :88-102 | **func at :89; preconditions at :92 and :100** | ✅ CONFIRMED (fixture is real) | :92 `require.False(..., "fixture precondition: a cancel-started walk is DYING (spawnsNewWork == false)")`, :100 asserts the incident. NOT a vacuous assertion. |
| C45 | plan phase table "P4 \| 0180 \| `engine`, `runtime`, `service`, `transport/http`" | 4 pkgs | **≤3 likely** | ⚠ over-listing (harmless) | `service` already maps `ErrConflict` → 409 at the transport layer, so `transport/http` probably needs no edit; listing it only widens the verify command. Contrast with C4, where a package is **missing**. |
| C46 | spec §1 "`STABILITY.md` puts the module pre-1.0 with **no released tag**" | no tag | **no version tag** | ✅ CONFIRMED | STABILITY.md:6-8 "pre-1.0, unreleased … no released version yet (no `v0.x` or `v1.x` git tag)"; `git tag` lists only `audit*` tags. |
| C47 | spec §3 "`re-dispatched=[]` vs `[undoB undoA]` … belongs to ADR-0175's abandon verb (`engine/step_compensation.go:1321-1326`)" | :1321-1326 | **:1321-1326 (measurement text at :1325)** | ✅ CONFIRMED | "(measured `[undoB undoA]` with undoB already run)" sits in `abandonCompensationWalk`'s retain comment, not on any `ActionFailed` path. |
