# Go build / test artifacts
/bin/
/dist/
*.exe
*.test
*.out
cover.out
coverage.*

# Go workspace
go.work
go.work.sum

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code local (machine-specific) overrides — shared config stays tracked
.claude/settings.local.json
.claude/projects/
.claude/todos/
.claude/scheduled_tasks.lock

# Subagent-driven-development scratch (briefs, reports, ledger, review packages)
.superpowers/

# Compiled example binaries dropped at repo root by `go build ./examples/<name>/`
# (leading slash anchors to root so the examples/<name>/ source dirs stay tracked)
/broker_wiring
/builtin_actions
/chaining_wiring
/migrate
/mysql_wiring
/production_wiring
/readme_quickstart
/real_db_transaction
/scenarios
/sqlite_wiring
# examples/scenarios/<name> build outputs (basename == scenario dir name)
/admin_monitoring
/attribute_authz
/boundary_timer
/call_activity
/compensation_saga
/exclusive_routing
/human_task_approval
/inclusive_gateway
/instance_cancellation
/inwait_reminder
/message_correlation
/parallel_fork_join
/retry_recovery
/signal_broadcast
/subprocess_embedded
